# Application Configuration
app:
  name: autogit-test
  version: 2.0.0
  debug: false

auth:
  session_timeout: 3600
  max_failed_attempts: 5
  lockout_duration: 900

database:
  host: localhost
  port: 5432
  name: testdb
